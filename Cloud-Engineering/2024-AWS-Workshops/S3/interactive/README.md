
```shell
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
which python
jupyter notebook --version
```

```shell
export BUCKET_NAME="smf2659-test-0"
export AWS_DEFAULT_PROFILE="personal-sso-admin"
```
